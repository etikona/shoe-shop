import React from 'react';
import './Choose.css'

const Choose = (props) => {
    const {chooseItem} = props.chooseItem;
    console.log(chooseItem);
    return (
        <div>
            
        </div>
    );
};

export default Choose;